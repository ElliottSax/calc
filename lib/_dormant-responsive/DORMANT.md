# Dormant: viewport detector

**Moved here:** 2026-09-06, during a dead-code sweep.

`viewport-detector.tsx` has no importers anywhere in the codebase.

**Decision needed:** finish-and-launch (wire it in wherever responsive
behavior currently relies on CSS breakpoints alone, if JS-side viewport
detection is actually needed somewhere) or delete. Until decided, leave
dormant.
